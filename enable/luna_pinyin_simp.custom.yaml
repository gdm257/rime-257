# luna_pinyin.custom.yaml
# luna_pinyin_simp.custom.yaml
# to do list:
# 標點符號（已完成）
# 快捷鍵
# 已知 bug：標點符號的「`」無效

patch:
  # punctuator:
    # half_shape:
      # "~": "~"
      # "`": "`"
      # "!": {commit: "！"}
      # "@": "@"
      # "#": "#"
      # "$": "$"
      # "%": "%"
      # "^": "^"
      # "*": "*"
      # '(' : '（'
      # ')' : '）'
      # "_": "_"
      # "=": "="
      # "|": "|"
      # '\\': "、"
      # "[": "【"
      # "]":  "】"
      # "{": "{"
      # "}": "}"
      # ":": {commit: "："}
      # ";": {commit: "；"}
      # "?": {commit: "？"}
      # ',': {commit: ， }
      # '.': {commit: 。 }
      # "/": "/"
      # "<": "《"
      # ">": "》"
      # "'": {pair: ["「", "」"]}

  "translator/dictionary": luna_pinyin.extended
