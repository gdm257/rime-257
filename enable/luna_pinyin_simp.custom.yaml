# luna_pinyin.custom.yaml
# luna_pinyin_simp.custom.yaml
# to do list:
# 標點符號（已完成，但弃用，请使用 default 的标点）
# 快捷鍵

patch:
  "translator/dictionary": luna_pinyin.extended

  # punctuator:
    # half_shape:
      # "~": "~"
      # "`": "`"
      # "!": {commit: "！"}
      # "@": "@"
      # "#": "#"
      # "$": "$"
      # "%": "%"
      # "^": "^"
      # "*": "*"
      # '(' : '（'
      # ')' : '）'
      # "_": "_"
      # "=": "="
      # "|": "|"
      # '\\': "、"
      # "[": "【"
      # "]":  "】"
      # "{": "{"
      # "}": "}"
      # ":": {commit: "："}
      # ";": {commit: "；"}
      # "?": {commit: "？"}
      # ',': {commit: ， }
      # '.': {commit: 。 }
      # "/": "/"
      # "<": "《"
      # ">": "》"
      # "'": {pair: ["「", "」"]}
